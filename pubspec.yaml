name: sentinelle_app
description: A Smart Tourist Safety App

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

cupertino_icons: ^1.0.6

firebase_core: ^2.30.0
firebase_auth: ^4.19.0
cloud_firestore: ^4.17.0
firebase_database: ^10.5.0
geolocator: ^11.0.0
google_maps_flutter: ^2.6.0
sensors_plus: ^5.0.1
path_provider: ^2.1.3
record: ^5.0.4

dev_dependencies:
flutter_test:
sdk: flutter

flutter_lints: ^3.0.0

flutter:
uses-material-design: true
